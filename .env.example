# ── Auth ──────────────────────────────────────
# Secret key for JWT signing (use a long random string)
JWT_SECRET=your-super-secret-jwt-key-here

# ── Database ───────────────────────────────────
# SQLite DB file path (Railway: set to Volume mount path)
# DB_PATH=/data/overtime.db
